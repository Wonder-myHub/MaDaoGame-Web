# 2026-06-22 工作日志

## UML 类图生成
- 基于代码探索结果生成了 MaDaoGame-Web 项目的完整 UML 类图
- **输出文件**：
  - `madao-game-uml.svg` — SVG 矢量图，可直接在浏览器查看
  - `madao-game-uml.puml` — PlantUML 源文件，可用 IDE 插件编辑和修改
- **覆盖范围**：8 个核心 Java 类 + 所有关系（依赖/聚合/组合）+ Spring 注解标注
- **类图内容**：
  - 实体层：Player (12字段, getter/setter)、GameRoom (11字段, 业务方法)
  - 控制器层：GameController (2注入, 14请求映射)
  - 服务层：GameService (核心逻辑, 8种行动类型)
  - DAO层：GameDao、PlayerDao (CRUD, PreparedStatement)
  - 工具层：DBUtil (HikariCP, 自动建表)
  - 启动类：MadaoGameApplication (@SpringBootApplication)
- 由于环境中 Java 不可用，PlantUML 渲染失败，改为直接手绘 SVG 方案
- **Bug 修复**：SVG 注释中的 `--`（双连字符）违反 XML 规范导致浏览器解析错误，已全部替换为 `→`（6处修复）
- **Bug 修复（箭头坐标）**：5处关系连线的起止坐标与类框边界不对齐，已修正：
  1. GameRoom◆→Player: 起点从内部(335,250)移至左边界(310,390)
  2. GameController→PlayerDao: 起点y从770修正为742(垂直中线)
  3. GameService→GameDao: 从斜线改为垂直线(165→165)，直对GameDao中心
  4. GameService→PlayerDao: 从斜线改为垂直线(425→425)，直对PlayerDao中心
  5. GameService◇→GameRoom: 从偏左x=280改为对角线(290→422)，菱形移至GameService侧
- **Bug 修复（图例+文字重叠+连线）**：
  - 图例"组合"图标从三角形改为正确菱形(4点)
  - 图例从左侧移至右侧(x=580)并垂直排列，消除与实体包重叠及水平文字溢出
  - GameRoom→Player 和 GameService→GameRoom 菱形改用内联 polygon 替代 marker，精确定位
  - MadaoGameApplication 注解文字拆分为两行，避免溢出 240px 框
